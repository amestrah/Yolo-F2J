services:
  - type: web
    name: yolov8-detection
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      PYTHON_VERSION: "3.8"  # Spécifiez votre version de Python si nécessaire
      PORT: 8080  # Port que Render doit utiliser
